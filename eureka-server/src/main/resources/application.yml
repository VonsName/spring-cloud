server:
  port: 8761
eureka:
  client:
    fetch-registry: false #不从注册中心获取自己的服务
    register-with-eureka: false #不在注册中心注册自己
    service-url:
      defaultZone: http://localhost:8761/eureka #指定注册中心地址
  instance:
    hostname: eureka-server #服务名称
